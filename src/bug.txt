defaultChecked={checked} is not working on NewPost.jsx for the checkbox input

delete post on profile page is not auto-re-rendering

NAVBAR COMPONENT - this not working
  useEffect(() => {
    if (window.localStorage.getItem('token')) {
      setIsLoggedIn(true);
    }
  }, []);

showEditPost applies to all posts in profile, instead of just one

profile messages SEE POST btn should take it to single poll page

add EDIT POST btn to singlepost and allPosts container

need to resize the textarea on the post reply form